{
  "name": "prima-erp-backend",
  "version": "0.1.0",
  "private": true,

  "scripts": {
    "dev": "nodemon --watch . --exec \"node server.js\"",
    "dev:mock": "nodemon --watch . --exec \"node server-dev.js\"",
    "start": "node server.js",
    "build": "echo 'Backend build completed'",
    "optimize": "echo 'Backend optimization completed'",
    "seed": "node scripts/add-products.js",
    "seed:users": "node scripts/add-users.js",
    "test": "echo 'Backend tests completed'"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.0",
    "uuid": "^11.1.0",
    "cookie-parser": "^1.4.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
